---
description: 当进行项目规划时，读取此文档。
alwaysApply: false
---
当你进行项目规划时，遵循以下规则：
# 1.请你在进行规划前，上网搜索对应模块功能的实现方式，确定你选择的实现方式没有过时。尤其是关于LLM的部分。搜索完之后，依据搜索到的内容确定你的项目规划。
# 2.项目规划包含且仅包含以下内容：项目建模，模块细表，项目整体架构。
# 3.项目建模：对如何实现需求做详细的逻辑梳理，构建一个合适的模型。此部分应该详实且娓娓道来。
# 4.模块细表：按照需求文档，为每一个功能模块进行具体设计，包括其职责，职责的具体实现思路和实现该职责使用的技术栈。具体实现思路一定要具体，每个模块的思路不少于300字。技术栈选择尽可能少，能用一个不用多个。
# 5.项目整体架构需要绘制mermaid图，绘制一张项目-用户动线Mermaid图。项目动线注重于用户操作传入程序后，在程序中会经历哪些模块。
# 6.架构设计原则：
## 微服务架构：模块为基本单位，基于功能职责分离每一服务模块，每个模块拥有自己独立的前端-后端-数据库。
## 单一职责：每个服务专注特定功能，互相间尽量无联系。
## 低耦合：服务间通过接口通信
## 可扩展性：支持水平和垂直扩展
## 强约束：不允许出现需求中不存在的非功能性设计
# 7. 每个模块要指出解决了需求文档中的哪些需求。
# 8. 项目规划的实现逻辑叙述要具体详实，模块与模块间尽管不能有联系，但也不能有断裂残缺的待实现部分。

不准设计：冗余管理，安全管理，数据库缓存。